<% (Tag.all).each do |tag| %>
 <li>
  <ul>
    	<%= link_to "##{tag.name}", tag_path(tag.name) %>
    		<small>
    			<%= link_to 'Delete', tag_path(tag), method: :delete %>
    		</small>
   	</ul>
<% end %> 


